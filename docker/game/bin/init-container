#!/bin/bash
set -euo pipefail

chownif() {
  local TARGET_IF="$1"
  if [[ "${TARGET_IF}" != "" ]]; then
    chown -R "$GAME_UID:$GAME_GID" "$TARGET_IF"
  fi
}

chownif "${CHOWN_GAME_DATA_DIR:-}"
chownif "${CHOWN_GAME_CONFIG_DIR:-}"
